version: '3.8'

services:
  oet-mcp:
    image: oet-mcp-server
    container_name: oet-mcp-server
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - MCP_PROXY_DEBUG=true
